(function (window) {
    //setTimeout(code, 1000);
    function code() {
        if (confirm("Print This page? It is Plugin Script")) {
            system.print(null);
        }
    }
})(window);